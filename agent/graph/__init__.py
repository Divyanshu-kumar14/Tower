"""TOWER slot graph package (T-01 stub).

Deterministic collision engine lives here (T-03: `slot_graph.py`).
This stub freezes the package boundary so cross-team imports
(`from agent.graph import ...`) resolve before implementation.
"""
